<?php

/**
 * These are configurations for the out of the box
 * administrator site that comes with saqle.
 * */

return [
     /**
      * Administrator url prefix:
      * 
      * All admin urls will have this prefix. The prefix
      * is added automatically to admin urls.
      * */
     'url_prefix' => '/backoffice',

     /**
      * Administrator module name.
      * 
      * All the admin code will be generated here!
      * */
     'module' => 'backoffice',

     /**
      * The model classes to add to the admin site
      * 
      * Only the models listed here will appear in admin site
      * */
     'models' => [

     ]
 ]
?>